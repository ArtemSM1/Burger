import styled from "styled-components";
import promo from "./promo-11.jpg"

const StyledPromo_order = styled.section `
    max-height: 760px;
    background-image: url(${promo});
    background-size: cover;
    background-position: center;
    padding-top: 100px;
    padding-bottom: 60px;
`

export default StyledPromo_order